cd mexc-sculp
docker compose down
cd ~
rm -rf mexc-sculp
git clone http://github.com/mybox1407-art/mexc-sculp.git
cd mexc-sculp
npm install protobufjs
npm install -D @types/protobufjs
docker compose up -d --build
npm install

docker compose logs -f mexc-scalper-bot
